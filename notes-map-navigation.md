# تشخيص اختفاء الخريطة عند التنقل

## دليل الفيديو

تحليل الفيديو المحلي `1000320735.mp4` أظهر أن الانتقال يتم من واجهة العميل إلى سجل النشاط ثم العودة، وأن صفحة الحساب تعيد ظهور الخريطة عند العودة للرئيسية. لا يظهر في المشهد تعطل شبكة أو طبقة شفافة؛ التبديل يزيل الشاشة الحالية ويعيد تركيب الشاشة التالية.

## قراءة الكود الحالي

`Customer` يحتفظ بحالة `page` داخل المكوّن، ويستخدم `navigateCustomer` لتغييرها. شاشة `order` فقط تستدعي `OptimusMapScreen`، بينما `home` تعرض بطاقة إنشاء الطلب ولا تستدعي `HamaMap`، و`orders` و`profile` تستبدلان الشاشة بالكامل. `HamaMap` يحمل مكوّن MapLibre ديناميكياً داخل `useEffect([])`؛ عند كل mount جديد يبدأ `MapStatus` loading ثم يستورد `hama-map-maplibre.native`.

## فرضية العمل

المشكلة ليست في OpenFreeMap أو GPS، بل في دورة تركيب شاشة الخريطة/حالتها عند العودة أو في أن شاشة الرئيسية الحالية لا تحتفظ بمكوّن الخريطة بعد تبديل الصفحة. يجب اختبار مسار Home → Orders → Home، ثم إصلاح mount/visibility أو إعادة القياس عند العودة، مع إبقاء MapLibre مركباً أو إعادة تهيئته بشكل صريح وآمن دون تغيير نظام الخريطة.

## التحقق البصري من الإطارات

الإطار الأول يعرض بطاقة `orderHero` كاملة. بعد العودة من «الطلبات» إلى «الرئيسية» تبقى الحاوية الخضراء الفاتحة، لكن محتواها الداخلي بالكامل — العنوان والزر واختصار الخريطة — يختفي، لذلك تبدو كأنها خريطة بيضاء/فارغة. بعد المرور بالحساب والعودة، يعود المحتوى. هذا يطابق مشكلة LayoutAnimation أثناء استبدال صفحات العميل أكثر مما يطابق فشل MapLibre أو البلاطات. الحل الآمن هو إزالة LayoutAnimation من `navigateCustomer` مع إبقاء التنقل والحاوية والخريطة كما هي.
