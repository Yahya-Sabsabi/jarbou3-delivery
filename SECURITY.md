# سياسة أمن مستودع جربوع

## النطاق

هذه السياسة تضبط ما يمكن حفظه في مستودع GitHub الخاص بالمشروع. المستودع هو نسخة احتياطية للشفرة والبنية والترحيلات والوثائق، وليس قاعدة بيانات تشغيلية ولا مخزناً لبيانات المستخدمين.

## قواعد غير قابلة للتفاوض

1. **لا تُرفع الأسرار.** تُستبعد ملفات `.env` و`.env.*` والمفاتيح والشهادات وملفات الأسرار عبر `.gitignore`.
2. **لا تُرفع بيانات المستخدمين.** يشمل ذلك أرقام WhatsApp، صور الهوية، الوثائق، مواقع GPS، رموز OTP، سجل الطلبات، والتقارير التشغيلية.
3. **لا تُطبع الأسرار في السجل.** لا تضع رموزاً أو كلمات مرور في `console.log` أو رسائل الأخطاء أو الاختبارات.
4. **الأذونات بأقل قدر لازم.** يظل المستودع خاصاً، ويمنح الوصول لأشخاص محددين يحتاجون فعلياً إلى تطوير المشروع.
5. **دوّر أي سر متسرب.** إذا دخل مفتاح إلى Git ولو لفترة قصيرة، اعتبره مكشوفاً، أبطله من المزود، وأنشئ بديلاً جديداً.

## طريقة إدارة الإعدادات الحساسة

ملفات البيئة ليست شيئاً يجب «تشفيره ثم رفعه» مع الكود؛ الأفضل ألا ترفع أصلاً. تستخدم بيئة النشر مدير الأسرار الخاص بها، وتستخدم مهام GitHub الآلية—إن أضيفت لاحقاً—**GitHub Secrets**. لا يُسمح للعميل أو التطبيق المحمول بحمل مفاتيح خدمة ذات صلاحيات مرتفعة.

## مراجعة إلزامية قبل الرفع

```bash
git status --short
git ls-files | grep -E '(^|/)(\.env|.*\.(pem|key|p12|p8|jks|mobileprovision)|.*\.(secret|secrets))$' || true
pnpm check && pnpm test && pnpm build
```

راجع أيضاً الملفات الجديدة يدوياً، خصوصاً مجلدات `docs/` و`supabase/migrations/` و`admin-site/`. لا ترفع ملفات مؤقتة أو نواتج بناء أو ملفات تصدير PDF للبيانات التشغيلية.

## التعامل مع الحوادث

عند الاشتباه في كشف سر أو وصول غير مصرح به: أوقف المفتاح أو الجلسة، دوّر الاعتماد، راجع سجل الوصول، وحدد نطاق الأثر قبل استئناف النشر. لا يكفي حذف الملف من commit لاحق؛ النسخ السابقة قد تبقى قابلة للوصول.
