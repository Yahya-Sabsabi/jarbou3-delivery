# تدقيق GitHub ونظام الرصيد — 2026-09-04

## GitHub

المستودع الخاص هو [Yahya-Sabsabi/jarbou3-delivery](https://github.com/Yahya-Sabsabi/jarbou3-delivery). آخر commit في GitHub وقت التدقيق هو `89a476c18cbb976ff974f13f9d51e313a4fd1647` بتاريخ 2026-08-31، بينما نسخة المشروع المحلية الحالية التي تحتوي سياسة OPTIMUS X هي `821b17c0cfc9dbacea023d62fbf8ecf912dd69be` بتاريخ 2026-09-04. لذلك تعديلات السياسة الأخيرة ليست مرفوعة بعد إلى GitHub.

## نظام الرصيد الحالي

يوجد دفتر حركات ذري في `supabase/migrations/20260901_jarbou3_driver_wallet_ledger.sql` يضم حساب الرصيد، الحجز، والخصم النهائي. دالة `record_driver_wallet_deposit` تتحقق من صلاحية المدير، وتتحقق من المبلغ بين 1 و1,000,000,000 ل.س، وتقفل حركة السفير بقفل معاملات، ثم تضيف حركة `deposit`.

تفرض دالة `driver_can_receive_order` حداً أدنى قدره 10,000 ل.س من الرصيد المتاح، وتتحقق من تغطية العمولة التقديرية بعد انتهاء الإعفاء. عند التسليم تُحسب العمولة من السعر النهائي، وتُستهلك الحجز، وتُسجل حركة `commission` سالبة.

مسار الإدارة `GET /admin/api/wallets` و`POST /admin/api/wallets/:driverId/deposits` موجودان ومحميان بجلسة بوابة الإدارة، كما أن واجهة `renderWallets` كانت تحتوي نموذج تعبئة، لكن تسمية الإجراء لم تكن واضحة بما يكفي. تم تغييرها إلى «تعبئة رصيد السفير» و«تعبئة الرصيد» مع توضيح Cash ودفتر الحركات، دون تغيير منطق الخادم.

## فجوات تحتاج تحققاً لاحقاً

لم يُحفظ بعد commit جديد في GitHub لهذه الجولة. كما ينبغي اختبار الإيداع فعلياً بجلسة مدير، والتحقق من ظهور الحركة في دفتر الرصيد، ثم إضافة اختبار ثابت لمسار تعبئة الواجهة إذا ظل ضرورياً. لم تُغيّر قواعد البيانات أو النسبة في هذه الجولة لأن المنطق الحالي يثبت 10% والحجز والخصم الذريين، ويجب عدم إضافة مسار مالي موازٍ قبل اختبار الموجود.
