# نتائج فحص Expo Go — 2026-09-03

الرابط الظاهر في صورة الهاتف هو `http://8081-ifp6v4ashv374quahhoms-6ed5dae0.us5.manus.computer`، وهو مختلف عن جلسة المشروع الحالية؛ لذلك فهو رابط QR قديم أو من جلسة أخرى.

بعد إعادة تشغيل المشروع، ظهر رابط الجلسة الحالية `https://8081-iwggi22bj0c0rjvhkctkh-285caf52.us1.manus.computer`. الاختبار من الطرفية عبر curl انتهى بمهلة بلا استجابة، بينما فتح الرابط في متصفح البيئة أعاد صفحة Expo Web تستجيب وتبقى على مؤشر تحميل. هذا يدل على أن proxy العام ليس مغلقاً تماماً، لكن الرابط لا يقدم مسار Metro المناسب لتشغيل Expo Go الأصلي أو أن الجلسة الحالية تعمل بوضع web/lan غير صالح للهاتف.

سجل Metro بعد إعادة التشغيل يثبت أن Metro بدأ على المنفذ 8081 وأن API بدأ على المنفذ 3000، بلا خطأ bundling أو TypeScript. سكربت `dev:metro` الحالي يستخدم `expo start --lan --clear --max-workers 1` دون تثبيت عنوان Sandbox قديم.

النتيجة المؤقتة: المشكلة ليست في كود التطبيق ولا في توقف Metro محلياً، بل في آلية الوصول من Expo Go إلى رابط جلسة Sandbox/Proxy. يلزم استخدام QR صادر من الجلسة الحالية فقط، وإذا لم يعمل فالأكثر استقراراً هو APK Release بدلاً من Expo Go، لأن Release لا يعتمد على Metro.


اختبار `expo start --tunnel` على منفذ مستقل فشل برسالة `ngrok tunnel took too long to connect`. لذلك لا يُنصح بتحويل إعداد المشروع إلى Tunnel كحل تلقائي؛ سيضيف فشلاً جديداً بدلاً من معالجة رابط الجلسة القديم.
